# Example environment variables for film-scout-frontend
# Copy this to `.env` and fill in real values before running locally or pushing secrets.

# Main API used by the frontend (development default shown)
VITE_API_URL=http://localhost:4000

# News API used by the News pages. Use a full URL to your deployed function or a local proxy.
# Example: https://us-central1-your-project.cloudfunctions.net/getNews
VITE_NEWS_API_URL=http://localhost:8080/getNews

# Environment identifier
VITE_ENVIRONMENT=development

# Optional feature flags
# VITE_ENABLE_FAKE_NEWS=false
